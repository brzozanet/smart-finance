{
  "name": "promptly-photo-ai",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "dependencies": {
    "cp": "^0.2.0",
    "npm-run-all": "^4.1.5"
  },
  "scripts": {
    "frontend": "npm run dev --workspace=frontend",
    "backend": "npm run dev --workspace=backend",
    "dev": "npm-run-all --parallel frontend backend"
  }
}
